import { Config } from '../commands/args';
export declare const config: {
    type: string;
    marker: string;
};
export declare const remarkPlugins: () => any[];
export declare const rehypePlugins: (config: Config) => any[];
