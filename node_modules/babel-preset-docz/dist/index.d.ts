export interface PresetOptions {
    flow?: boolean;
    typescript?: false;
    parseProps?: boolean;
}
declare const preset: (api: any, opts?: PresetOptions) => {
    presets: any[];
    plugins: any[];
};
export default preset;
